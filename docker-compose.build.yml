# Source build. Layer over the base file:
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d
# The result is tagged kypassword-server:local so it never occupies the published name.
services:
  kypassword-server:
    image: kypassword-server:local
    pull_policy: build
    build:
      context: .
      dockerfile: Dockerfile
